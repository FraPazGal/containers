file:
  /opt/bitnami/{{ .Vars.app_name }}:
    filetype: directory
    exists: true
  {{ range $directory := .Vars.filesystem.directories }}
  {{ $directory }}:
    exists: true
    filetype: directory
    mode: "0755"
  {{ end }}
