file:
  /opt/bitnami/{{ .Vars.app_name }}:
    filetype: directory
    exists: true
  {{- $mode := .Vars.filesystem.mode }}
  {{ range $directory := .Vars.filesystem.directories }}
  {{ $directory }}:
    exists: true
    filetype: directory
    mode: "{{ $mode }}"
  {{ end }}
