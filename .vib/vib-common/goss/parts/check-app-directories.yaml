file:
  /opt/bitnami/{{ .Vars.app_name }}:
    filetype: directory
    exists: true
  # {{- $mode := "%s" .Vars.filesystem.mode }}
  {{ range $directory := .Vars.filesystem.directories }}
  {{ $directory }}:
    exists: true
    filetype: directory
    mode: "0666"
  {{ end }}
