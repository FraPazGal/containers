command:
  # Checks needed container's components are in PATH
  check-components:
    exec: export BITNAMI_ROOT_DIR={{ .Vars.root_dir }} && ./common/goss/scripts/check-components-binaries.sh
    exit-status: 0
